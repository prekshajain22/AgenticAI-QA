"""
Playwright automation agents sub-package.

Modules
-------
playwright_agent
    Standalone agent that takes a plain-text smoke test scenario and
    executes every step in a real browser via the Playwright MCP server.
    Reports PASS / FAIL per step and ends with TESTING COMPLETE.
"""
