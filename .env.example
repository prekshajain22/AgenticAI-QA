# ==========================================
# AGENTIC AI - QA Automation Framework
# Copy this file to .env and fill in values
# ==========================================

# Environment name (e.g. QA, staging, prod)
ENVIRONMENT=QA

# Base URL of the application under test
BASE_URL=https://www.saucedemo.com

# ==========================================
# Browser settings (Playwright)
# ==========================================
# Browser engine: chromium | firefox | webkit
BROWSER=chromium
# Run browser in headless mode: True | False
HEADLESS=True
# Slow-motion delay in milliseconds (0 = off)
SLOW_MO=0

# ==========================================
# Timeouts (milliseconds)
# ==========================================
DEFAULT_TIMEOUT=5000
DEFAULT_NAVIGATION_TIMEOUT=10000

# ==========================================
# AI model settings — pick ONE free provider
# ==========================================

# ── Option 1: Groq (free, no credit card — console.groq.com → API Keys)
# ⚠️  Use llama-3.1-8b-instant (20k TPM) not llama-3.3-70b-versatile (12k TPM)
#    Agentic workflows with MCP tool responses easily exceed 12k tokens/min.
GROQ_API_KEY=your-groq-api-key-here
GROQ_MODEL=llama-3.1-8b-instant

# ── Option 2: Google Gemini (1M TPM free — aistudio.google.com → Get API key)
# ⚠️  aistudio.google.com free tier is SEPARATE from Google Cloud trial credits.
#    Even if your Cloud credits expired, AI Studio is still free at 1M TPM.
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-2.0-flash

# ── Option 3: OpenAI (paid)
OPENAI_API_KEY=sk-proj-your-openai-api-key-here
OPENAI_MODEL=gpt-4o-2024-08-06

# MCPConfig.default_client() auto-selects: GROQ_API_KEY first, then GEMINI_API_KEY.


# ==========================================
# JIRA settings (used by jira_agent.py)
# ==========================================
JIRA_URL=https://your-org.atlassian.net
JIRA_USERNAME=your-jira-email@example.com
JIRA_API_TOKEN=your-jira-api-token-here
JIRA_PROJECT_KEY=CRED
JIRA_PROJECT_NAME=CreditBank

# ==========================================
# Flask service
# ==========================================
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET=your-flask-secret-key-here
